#include<bits/stdc++.h>
using namespace std;
class Solution {
public:
    int search(vector<int>& nums, int target) {
        int begin=0;
        int end=nums.size();
        for(;begin<=end;){
            int mid = 
        }
        return -1;
    }
};
int main(){

    Solution sol;
    vector<int> nums={2,5};
    int target = 5;
    cout<<sol.search(nums,target);

    return 0;
}